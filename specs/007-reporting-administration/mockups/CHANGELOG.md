# Changelog — F-007 Mockups

## 2026-08-07 — Initial Generation (mode: generate)

- Created `vacation-report-default.html` — report with 5-column filter bar, paginated results table, export buttons
- Created `audit-trail-default.html` — searchable audit log with 5 sample entries (user actions + system events)
- Created `admin-config-default.html` — inline config table with global and department-specific threshold overrides
- Created `user-management-default.html` — user table with inline role selector, active/inactive status, deactivate action
